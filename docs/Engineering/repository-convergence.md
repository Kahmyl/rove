# Repository Convergence Record

**Date:** 15 September 2026

**Starting `main`:** `eff089e63d23799d97cebd08b7c4a62ddcaaf1ed`

**Accepted source-content candidate:** `9fc63b56c1c80902073a1303cbbfa8800483ee83`

**Final `main`:** the commit targeted by annotated tag `checkpoint-post-worktree-convergence-20260915`; the exact value is reported after the final fast-forward because a commit cannot embed its own object identity.

This one-time convergence made GitHub `main` the repository truth again. Worktree isolation was treated as temporary execution state, not as a competing product or architecture source.

## Preservation snapshots

| Work body                   | Preservation commit                        | Annotated archive tag                               |
| --------------------------- | ------------------------------------------ | --------------------------------------------------- |
| Primary brand/coherence     | `37a2349d769874c98ae366bb368dea61e1271d60` | `archive/pre-convergence-brand-coherence-20260915`  |
| Integration UI/product      | `fdf9a186de536188ccb44832d2a8880b6200c3e7` | `archive/pre-convergence-integration-ui-20260915`   |
| Historical customer journey | `1772f56fab4a9e05fa5364a288a82b922e2f2759` | `archive/pre-convergence-customer-journey-20260915` |
| Workflow synchronization    | `37e213a10c5c0dccc50b4de0b991ad1d2545d36d` | `archive/pre-convergence-workflow-sync-20260915`    |

Each branch and tag was pushed and verified against the remote before reconciliation.

## Reconciled work

- The primary brand patch contributes the terracotta identity/current/focus language, semantic success/warning/danger separation, responsive product-health treatment, and brand evidence harness. It was layered on the later integration UI rather than restoring its older task surface.
- The integration UI patch moves archived tasks out of ordinary sidebar history into Settings, retains read/restore behavior and attention visibility, adds profile/settings navigation, and updates the owning product and capability contracts.
- The historical journey snapshot was classified rather than copied. Its package command already exists in evolved form; its first-launch script is superseded by the committed deterministic Journey 01; and its generic walker/test are superseded by the journey-specific and UI Truth harnesses. The archive tag retains the exact historical implementation.
- The Workflow-sync candidate contributes the optional account boundary, Supabase adapter and migration, owner-partitioned local ledger, portable projection, CAS/idempotency, cursors, tombstones, durable pending operations, conflict resolution, account switching, export/disclosure, Settings states, and deterministic qualification tooling. Current task lifecycle, Output authority, Runtime control authority, Workflow workspace behavior, and current Codex component selection remain authoritative.

Workflow synchronization is not a startup prerequisite or an enabled production service. Without explicit Supabase URL and publishable-key configuration, no provider client, synchronization coordinator, or monitor is created. Local tasks and Workflows remain available without an account or network. Task conversations, Outputs, actions, recordings, screenshots, downloads, attachments, credentials, cookies, browser/live state, local paths, approvals, and execution state remain outside the portable schema. Production provider authority and live/deployed qualification remain open.

## Branch and commit accounting

The reconciliation source commits map to the integrated main commits as follows:

| Source    | Integrated main | Evidence                                                                                        |
| --------- | --------------- | ----------------------------------------------------------------------------------------------- |
| `ecc9b30` | `68a28d5`       | identical stable patch ID                                                                       |
| `32ca065` | `5364cc1`       | identical stable patch ID                                                                       |
| `e1182bf` | `de75989`       | range-diff; integrated form retains newer exact-control wording                                 |
| `57dd9db` | `1be8f27`       | range-diff and identical changed-path set; integrated form retains newer control-schema context |
| `20dd094` | `01e0b0a`       | identical stable patch ID                                                                       |
| `2b5f28e` | `7c3a475`       | identical stable patch ID                                                                       |
| `4959344` | `ddd1d9c`       | identical stable patch ID                                                                       |

The registered engineering-environment, MVP-completion, task-lifecycle, browser-perception, external-Chrome, handoff, capture, transport, and browser-policy branches were all ancestors of the candidate. The three reconciliation branches above are superseded by their integrated commits and were not re-merged.

Reported unreachable commits were also accounted for: `20fec660` and `240d9dde` are patch-identical predecessors of `02893742` and `2a605b3e`; `0728f577` and `10d129d5` are earlier amended Workflow-journey candidates superseded by `201eb576`; and `d6078644` and `3aa78e3b` are earlier runtime candidates superseded by `19af4899`, whose final form adds stronger provenance handling and control-return coverage.

Verification commands and the exact final `main`, tree, remote, and cleanup state are retained in the final convergence report and post-convergence tag.
